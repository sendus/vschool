var ask = require('readline-sync');  
var input = ask.question('What phrase would you like to encrypt? ').toLowerCase();  
var shift = parseInt(ask.question('How many letters would you like to shift? '));
//var alph = "abcdefghijklmnopqrstruvwxyz".split("");
//function changeLetter() {
    //
//}
/*for (var i=0; i<input.length; i++){
    var index = alph.indexOf(input[i]);
  if(alph.indexOf(input[i]) != -1){*/
   // return input.fromCharCode() + shift.indexOf(shift[i]);
    /*  return input+=shift;
} else {
    return input;
      }
}  
}*/
      
function changeLetter() {
    if (input.charCode )
}